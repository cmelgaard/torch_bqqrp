[build-system]
requires = [
    "setuptools>=61",
    "wheel",
    "torch>=2.0.0",   # allows 2.5.1+cu121
    "numpy>=1.24",    # allows 2.5.3 (no upper bound here)
]
build-backend = "setuptools.build_meta"
